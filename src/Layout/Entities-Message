{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Message",
  "description": "Message exchanged during a consultation",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "consultation_id": {
      "type": "string",
      "description": "Reference to the consultation"
    },
    "role": {
      "type": "string",
      "enum": [
        "user",
        "fortune_teller"
      ],
      "description": "Who sent the message"
    },
    "content": {
      "type": "string",
      "minLength": 1,
      "description": "Message content"
    },
    "is_follow_up": {
      "type": "boolean",
      "default": false,
      "description": "Whether this message is a follow-up question"
    }
  },
  "required": [
    "consultation_id",
    "role",
    "content"
  ]
}
