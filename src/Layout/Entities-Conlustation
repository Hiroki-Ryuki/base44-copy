{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Consultation",
  "description": "User consultation record",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "user_id": {
      "type": "string",
      "description": "Reference to the user"
    },
    "topic": {
      "type": "string",
      "enum": [
        "love",
        "career",
        "health",
        "money",
        "relationships",
        "general"
      ],
      "description": "Consultation topic"
    },
    "concern": {
      "type": "string",
      "minLength": 1,
      "description": "User's specific concern or question"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "completed"
      ],
      "default": "active",
      "description": "Current consultation status"
    },
    "follow_up_count": {
      "type": "integer",
      "minimum": 0,
      "maximum": 2,
      "default": 0,
      "description": "Number of follow-up questions used (max 2)"
    },
    "summary": {
      "type": "string",
      "description": "Brief summary of the consultation"
    }
  },
  "required": [
    "user_id",
    "topic"
  ]
}
